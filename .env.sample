# AI DevSecBuddy — environment template (committed; no secrets).
# Copy to .env and fill in the secret(s):  cp .env.sample .env
# .env is gitignored; deploy.sh auto-loads it (set -a; . ./.env; set +a). To run
# uvicorn directly, source it the same way first so the engine sees these values.

# Default engine the backend runs (mock | anthropic | vertex | gemini).
#   mock        offline + free + deterministic
#   anthropic   Claude
#   vertex      Gemini on GCP (gcloud auth)'
#   gemini      Gemini via a URL/API-key gateway (no SDK)
DEVSECBUDDY_ENGINE=vertex

# ── Anthropic — Claude, called directly against the Anthropic API ──
# Anthropic API key (Claude). Get one: docs/setup/anthropic-signup.md
ANTHROPIC_API_KEY=
# Default model; selectable per run in the UI. The catalog (cheapest → priciest) lives in
# devsecbuddy/defaults/models.json — add models there:
#   claude-haiku-4-5 · claude-sonnet-4-6 · claude-opus-4-8 · claude-fable-5
DEVSECBUDDY_ANTHROPIC_MODEL=claude-haiku-4-5

# ── Google Vertex AI — Gemini models, via GCP ──
#   Authenticate once as your user (writes Application Default Credentials):
#     gcloud auth application-default login
#   then enable the Vertex AI API on the project and grant your account
#   roles/aiplatform.user.  See docs/setup/google-vertex-signup.md.
DEVSECBUDDY_VERTEX_PROJECT=devsecbuddy
DEVSECBUDDY_VERTEX_PROJECT_NUMBER=595277113347   # informational; the SDK uses the project ID above
DEVSECBUDDY_VERTEX_REGION=us-east1
# Default model; selectable per run in the UI. Catalog (cheapest → priciest) is in
# devsecbuddy/defaults/models.json — 2.5 + 3.x series:
#   gemini-2.5-flash-lite · gemini-2.5-flash · gemini-2.5-pro · gemini-3.1-flash-lite ·
#   gemini-3-flash · gemini-3.1-pro
DEVSECBUDDY_VERTEX_MODEL=gemini-2.5-flash
#   ^ If a run returns NOT_FOUND for the model, it isn't served from this region —
#     switch DEVSECBUDDY_VERTEX_REGION to us-central1 or global.

# ── Google Gemini via a URL/API-key gateway (engine "gemini") ──
#   A third way to reach Gemini: plain HTTPS to a gateway that takes an API key in a
#   header (no gcloud login, no SDK). See docs/setup/google-gemini-gateway.md.
#   GEMINI_BASE_URL is deployment-internal — there is NO default; set it yourself.
GEMINI_BASE_URL=
GEMINI_API_KEY=
# Standard, with sensible defaults (uncomment to override):
# GEMINI_API_KEY_HEADER=x-api-key             # header the key is sent in
# GEMINI_MODEL_NAME=gemini-2.5-flash          # default model; selectable per run in the UI
# GEMINI_LOCATION=us-central1
# GEMINI_PROMPT_URL=/api/v1/gemini/prompt     # path (or full URL) for the scoring call
# GEMINI_TIMEOUT_SECONDS=60
# Gateway-specific extras (optional; cost attribution). Leave GEMINI_CUSTOM_COST_TAG
# blank to disable — no tag is registered and none is sent with the prompt:
# GEMINI_CUSTOM_COST_TAG=
# GEMINI_CUSTOM_COST_TAG_URL=/api/v1/tag      # where the cost tag is registered

# ── Optional ──
# DEVSECBUDDY_DB=data/ledger.db                 # SQLite ledger path
# DEVSECBUDDY_CORS_ORIGINS=http://localhost:5173
